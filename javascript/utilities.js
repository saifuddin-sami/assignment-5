function getInnerText (id) {
    const innerTxt = document.getElementById(id).innerText; 
    return innerTxt;
}

function getInputValue (id2) {
    const inputValue = document.getElementById(id2).value;
    return inputValue;
}